spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://mysql:3306/subscription?useUnicode=true&characterEncoding=utf8&serverTimezone=UTC}
    username: ${DB_USERNAME:application}
    password: ${DB_PASSWORD:application}

logging:
  level:
    org.hibernate.SQL: info
